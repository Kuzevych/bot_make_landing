import { createStyles } from "@material-ui/core/styles";


export default function styles() {
  return createStyles({
    root: {},
    textContainer: {
      maxWidth: '50%'
    },
    image: {}
  });
}